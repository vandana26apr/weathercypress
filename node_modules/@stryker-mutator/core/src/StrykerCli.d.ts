import { Command } from 'commander';
import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
export default class StrykerCli {
    private readonly argv;
    private readonly program;
    private readonly runMutationTest;
    private readonly log;
    private command;
    private strykerConfig;
    constructor(argv: string[], program?: Command, runMutationTest?: (options: Partial<StrykerOptions>) => Promise<import("@stryker-mutator/api/src/report/MutantResult").default[]>, log?: Logger);
    run(): void;
}
//# sourceMappingURL=StrykerCli.d.ts.map