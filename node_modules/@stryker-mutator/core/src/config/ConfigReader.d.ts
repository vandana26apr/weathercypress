import { Config } from '@stryker-mutator/api/config';
import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
export declare const CONFIG_SYNTAX_HELP: string;
export default class ConfigReader {
    private readonly cliOptions;
    private readonly log;
    static inject: ["cliOptions", "logger"];
    constructor(cliOptions: Partial<StrykerOptions>, log: Logger);
    readConfig(): Config;
    private fixDeprecations;
    private getNewExcludedMutations;
    private loadConfigModule;
}
//# sourceMappingURL=ConfigReader.d.ts.map