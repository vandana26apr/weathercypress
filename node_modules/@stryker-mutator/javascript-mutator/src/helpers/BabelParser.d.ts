import * as types from '@babel/types';
import { Logger } from '@stryker-mutator/api/logging';
import { MutatorDescriptor } from '@stryker-mutator/api/core';
import { NodeWithParent } from './ParentNode';
export default class BabelParser {
    private readonly log;
    static inject: ["logger", "mutatorDescriptor"];
    private readonly options;
    constructor(log: Logger, mutatorDescriptor: MutatorDescriptor);
    parse(code: string): types.File;
    private createOptions;
    getNodes(ast: types.File): NodeWithParent[];
    generateCode(ast: types.Node): string;
}
//# sourceMappingURL=BabelParser.d.ts.map