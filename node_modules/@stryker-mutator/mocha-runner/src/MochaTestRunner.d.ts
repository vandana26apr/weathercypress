import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
import { RunResult, TestRunner } from '@stryker-mutator/api/test_runner';
export default class MochaTestRunner implements TestRunner {
    private readonly log;
    private readonly allFileNames;
    private testFileNames;
    private readonly mochaOptions;
    static inject: ["logger", "sandboxFileNames", "options"];
    constructor(log: Logger, allFileNames: readonly string[], options: StrykerOptions);
    init(): void;
    private mocha6DiscoverFiles;
    private legacyDiscoverFiles;
    private mochaFileGlobPatterns;
    run({ testHooks }: {
        testHooks?: string;
    }): Promise<RunResult>;
    private purgeFiles;
    private addFiles;
    private addTestHooks;
    private configure;
    private additionalRequires;
}
//# sourceMappingURL=MochaTestRunner.d.ts.map