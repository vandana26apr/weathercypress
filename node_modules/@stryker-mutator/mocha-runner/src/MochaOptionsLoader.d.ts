import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
import { MochaOptions } from './MochaOptions';
/**
 * Subset of defaults for mocha options
 * @see https://github.com/mochajs/mocha/blob/master/lib/mocharc.json
 */
export declare const DEFAULT_MOCHA_OPTIONS: Readonly<{
    extension: string[];
    file: never[];
    ignore: never[];
    opts: string;
    spec: string[];
    timeout: number;
    ui: string;
}>;
export default class MochaOptionsLoader {
    private readonly log;
    static inject: ["logger"];
    constructor(log: Logger);
    load(strykerOptions: StrykerOptions): MochaOptions;
    private loadMochaOptions;
    private loadMocha6Options;
    private loadLegacyMochaOptsFile;
    private readMochaOptsFile;
    private parseOptsFile;
    private parseNextInt;
    private parseNextString;
}
//# sourceMappingURL=MochaOptionsLoader.d.ts.map